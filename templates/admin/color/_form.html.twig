{{ form_start(form) }}
<div class="container">
    <div class="row  justify-content-center">
        <div class="col-12  mt-4">
            {{ form_label(form.name) }}
            {{ form_widget(form.name) }}
            {{ form_label(form.color) }}
            {{ form_widget(form.color) }}
        </div>
        <div class="  mt-4"><button class="btn btn-success mb-3">{{ button_label|default('Enregistrer') }}</button></div>
    </div>

    {{ form_end(form) }}
